type Movie {
  id: Int!
  title: String!
  rating: Float!
  year:Int!
  summary: String!
  language: String!
  medium_cover_image: String!
}

type Query {
  movies(limit:Int!,rating:Float!): [Movie]
  movie(id:Int!): Movie
  suggesions(id:Int!): [Movie]
}
